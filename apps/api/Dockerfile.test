FROM node:20-alpine

RUN corepack enable && corepack prepare pnpm@11.9.0 --activate

WORKDIR /app

COPY . .

RUN pnpm install
RUN pnpm --filter @my-llm/api exec prisma generate

CMD ["pnpm", "--filter", "@my-llm/api", "test"]
