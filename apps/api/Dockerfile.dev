FROM node:22-alpine

RUN corepack enable && corepack prepare pnpm@11.9.0 --activate

WORKDIR /app

# Copier l'ensemble du monorepo pour le linking workspace
COPY . .

RUN pnpm install
RUN pnpm --filter api exec prisma generate

EXPOSE 8000

CMD ["pnpm", "--filter", "api", "dev"]
