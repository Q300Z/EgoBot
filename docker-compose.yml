services:
  valkey:
    image: valkey/valkey:8-alpine
    container_name: egobot_valkey
    ports:
      - "6379:6379"
    command: valkey-server --appendonly yes
    volumes:
      - egobot_valkey_data:/data

volumes:
  egobot_valkey_data:

